package com.summers.artworkeditor

import android.content.Context
import android.graphics.Canvas
import android.view.View

class NewCanvas(context: Context) : View(context) {

    init {

    }

    override fun onDraw(canvas: Canvas?) {
        super.onDraw(canvas)
    }

}